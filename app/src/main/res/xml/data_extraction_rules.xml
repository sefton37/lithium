<?xml version="1.0" encoding="utf-8"?>
<!--
    Prevents ALL cloud backup and device transfer of app data.
    Notification content is sensitive — it must never leave the device
    through Android's backup/restore mechanisms.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
    </device-transfer>
</data-extraction-rules>
