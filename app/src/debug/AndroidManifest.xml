<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="ai.talkingrock.lithium.debug.SimulationActivity"
            android:exported="true" />
        <!--
            Minimal activity for Hilt Compose UI tests (Phase 4).
            Declared in the debug APK so createAndroidComposeRule can launch it
            in the app process (ai.talkingrock.lithium.debug) rather than the
            test process (debug.test). Activity itself renders nothing.
        -->
        <activity
            android:name="ai.talkingrock.lithium.HiltTestActivity"
            android:exported="false" />
        <receiver
            android:name="ai.talkingrock.lithium.debug.ScoringDevReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="ai.talkingrock.lithium.RUN_REFIT" />
                <action android:name="ai.talkingrock.lithium.DUMP_IMPLICIT" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
