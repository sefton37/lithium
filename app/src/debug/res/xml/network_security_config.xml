<?xml version="1.0" encoding="utf-8"?>
<!--
    DEBUG override: allows plaintext HTTP to the Android emulator's host-loopback
    alias (10.0.2.2) so the app can route LLM inference to Ollama running on the
    developer workstation. Production builds use the HTTPS-only base config.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
